# Validation Master Plan (VMP)

## Scope
{{ VMP.scope if VMP.scope else "To be defined by site." }}

## Roles and Responsibilities
{{ VMP.roles if VMP.roles else "Engineering, Validation, QA, Operations." }}

## Timeline
{{ VMP.timeline if VMP.timeline else "To be defined in project schedule." }}

## Deliverables
{{ VMP.deliverables if VMP.deliverables else "VMP, URS, FRS, TRS, IQ/OQ/PQ protocols and reports." }}

## Training Plan
{{ VMP.trainingPlan if VMP.trainingPlan else "Training completion required before protocol execution." }}

## Supplier Evidence Plan
{{ VMP.supplierEvidencePlan if VMP.supplierEvidencePlan else "Collect supplier manuals, certificates, FAT/SAT, and baseline docs." }}
